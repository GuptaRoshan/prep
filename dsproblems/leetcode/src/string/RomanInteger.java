package string;

class RomanInteger {

    // public static int covert() {

    // switch(){

    // }

    // return 0;
    // }

    public static void main(String[] args) {
        String s = "LVIII";

        for (int i = 0; i < s.length(); i++) {
            if (i + 1 < s.length()) {
                System.out.println(s.charAt(i));
            }
            else {
                System.out.println(s.charAt(i));
            }
        }
    }
}